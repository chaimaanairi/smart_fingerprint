module.exports = {
  // Plugins to use for PostCSS
  plugins: {
    // Tailwind CSS plugin - Generates the necessary styles based on the Tailwind configuration
    tailwindcss: {},

    // Autoprefixer plugin - Adds vendor prefixes to CSS for better browser compatibility
    autoprefixer: {},
  },
}
